let path = [];

function resolve(labyrinthe){
    // Partir case entrance
    // Avancer et marquer visited jusqu'a une intersection (node)
    // Si intersection, checker chaque chemin
    // Si cul de sac (plus moyen d'avancer, donc mur ou case visited), revenir au node et reiterer
    // Si case exit => ajouter les cases prises en memoire à l'array path
}
